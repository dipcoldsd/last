{
  "name": "mern-azure-deployment",
  "version": "1.0.0",
  "description": "MERN Stack deployment ready for Azure",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  }
}
